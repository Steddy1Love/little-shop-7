<%= form_with url: admin_merchant_path(@merchant), method: :patch, data: { turbo: false } do |f| %>
  <%= f.label :name, 'Name: ' %>
  <%= f.text_field :name, value: @merchant.name %>
  <%= f.submit 'Submit' %>
<% end %>
